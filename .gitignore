# === Rust ===
/target/          # Папка сборки (бинарники, зависимости)
**/*.rs.bk        # Бэкапы Rust-файлов (если редактор создаёт)
Cargo.lock        # Для библиотек (но включается для бинарников!)

# === IDE/Editor ===
.idea/            # JetBrains (RustRover, CLion)
.vscode/          # VS Code
*.swp             # Vim/Neovim временные файлы
*.swo
*.kate-swp

# === Системные файлы ===
.DS_Store         # macOS
Thumbs.db         # Windows
ehthumbs.db
Desktop.ini

# === Профили сборки ===
# (Опционально, если хотите игнорировать специфичные профили)
/target/

# === Документация ===
/doc/             # Локальная документация (`cargo doc`)

# === Бенчмарки и тесты ===
**/*.bench       # Файлы бенчмарков
**/*.profdata    # Данные профилирования

# === Тестовые сегменты ===
/segments/

# === Environment ===
.env              # Переменные окружения
*.local           # Локальные конфиги